/**
 * Environment variables that the application uses to function correctly.
 *
 * These variables are recognized at the production level.
 */
export const environment = {
  production: true,
  newsURLBase: 'https://hn.algolia.com/api/v1',
};
